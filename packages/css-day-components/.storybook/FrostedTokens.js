import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: `<img src="/assets/Logo.svg" alt="Frosted Tokens" style=max-width="250px" />`,
  brandTarget: '_self'
});
